<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.titech.itpro.sdl.frashcard">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SdlFrashcard">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SdlFrashcard.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateActivity" />
        <activity android:name=".test.TestMainActivity" />
        <activity android:name=".test.TestFrontActivity" />
        <activity android:name=".test.TestBackActivity" />
        <activity android:name=".test.TestBackChoiceActivity" />
        <activity android:name=".test.TestWithFriendHomeActivity" />
        <activity android:name=".test.TestWithFriendActivity" />
        <activity android:name=".test.TestWithFriendActivityReceiver" />
        <activity android:name=".test.TestWithFriendActivitySender" />
        <activity android:name=".test.connection.ScanActivity" />
    </application>

</manifest>